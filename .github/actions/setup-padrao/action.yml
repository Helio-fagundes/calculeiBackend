name: setup-padrao
author: helio
description: setup-padrao
runs:
  using: composite
  steps:

    - name: Configure Java 21
      uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: maven